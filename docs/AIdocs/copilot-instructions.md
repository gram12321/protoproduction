# Copilot Instructions

The canonical docs-side agent rules are now in `docs/AIdocs/airules.mdc`.

Cursor keeps a mirrored copy at `.cursor/rules/ai-agent-rule.mdc/airulesVS.instructions.md`.

Do not add separate rules here; update the canonical rule file and mirror it to Cursor instead.
