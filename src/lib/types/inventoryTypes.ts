import type { ResourceType } from "./resourceTypes";

export interface Inventory {
  grain: number;
  water: number;
  flour: number;
  sugarcain: number;
  sugar: number;
  bread: number;
  cake: number;
}
